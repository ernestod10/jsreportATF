

function nombre_apellido(nombre, apellido) {
    var completo = nombre+' '+apellido;
    return completo
}

function total(costo) {
    var sum = 0
    sum = costo+(costo*0.1);
    return sum;
}
function iva(costo) {
    return (costo * 0.1);
}

